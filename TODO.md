# TODO — odoo_engenapp

## Em curso
- **afr_cme_rastreabilidade — kiosk multi-station (Tasks 12-20)**: extender padrão kiosk Expurgo a Preparo + Esterilização (opt-in ICP) + Distribuição + substituir actions menu por kiosk-default + dashboard picker. **Entregue até agora:** Task 12 (refactor gating: getter `kioskStationEnabled`/`kioskUIReady`, `currentBlockReasons`/`primaryActionSpec`/`confirmCancelCurrent` com `_cancelRpcMap`, 3 ICPs per-station, `station_kiosk_enabled` em `app_config`, Esc fecha modal Retrabalho, drawer Pendências vazio com ícone ✓, cancelados fora de `totem_list_recent_receipts`, toast por estação), Task 13 (Preparo SetupPanelPreparo + PreparoReturnBlock amber + `preparoBlockReasons` real + render gates t-elif + backend merge `totem_add_preparation_line`), Picker dashboard (CmeTotemPicker action + tiles por estação + ✕ exit redirect picker + opt-out persist `sessionStorage` + `?kiosk=1` re-arma), Tasks 15+16+17 combinados (SterilizationChips ①②③ + SetupPanelEsterilizacao + SterilizationIndicators + state machine primary action contextual + `esterilizacaoBlockReasons` waiting_release + scan_code mono na MaterialsList + busca por scan_code parcial via novo RPC `totem_search_units_for_sterilization` + modifier `--with-chips` no split). **Restante:** Task 14 (audio feedback no return scan), 18 (Distribuição EntregaTargetBar + ScanHero disabled), 19 (RPCs `totem_cancel_*` preparo/ester/distrib), 20 (manual checklist + hints F-keys genéricos). Commits submodule: ca18611 → 61e22b7 → bf12cc7 → fc0af7e. Monorepo bumps: fe4f470 → 32115bc → ccedccc → 768a728. Plan: `/home/afonso/.claude/plans/ainda-vamos-ficar-no-partitioned-barto.md`. [[project-afr-cme-rastreabilidade]]

## Feito
- 2026-06-09 — **Polish sprint — done-guard board + mixin afr_qualificacao + doc submodules** (committed+pushed `main-monorepo`). **BUG agendamento (commit `d3c4ca1`):** visita `done` era editável/apagável/divisível pelo board (só `board_reschedule` protegia). Add helper `_board_check_not_done` em `board_split_overflow`/`board_delete_visita`/`board_set_hours` (UserError se done); frontend `visita_board.xml` esconde lixeira + faixa split e desabilita inputs hora em done. 3 testes novos (14/14 board verde). **afr_qualificacao polish (submodule `6ae56a9` PUSHED + bump `c68c06e`):** constraint duplicada `_check_product_is_service` (cycle/malha/optional) → AbstractModel `afr.product.service.mixin` (`models/product_service_mixin.py`); config_template ValidationError p/ topo + msgs em `_()`. 286 testes 0 regressão. **Doc (`5bd0ec0`):** CLAUDE.md corrigido — **6 submodules** (era 2) + alerta push-antes-do-bump. **E2E (agent-browser, qualif_test_f811):** visita done mostra badge ✓, sem lixeira, inputs disabled; não-done intacta.
- 2026-06-09 — **afr_qualificacao_agendamento — 3 regras de negócio + 2 polish + suite robusta + E2E** (db `qualif_test_f811`; committed `e12144f` `main-monorepo`). **Regras (model `os_visita.py`):** (1) **não programar no passado** `@api.constrains('date','state')` — bloqueia data < hoje (`context_today`), **isenta `done`** (registrar visita realizada passada é legítimo); (2) **OS em execução trava visita** — `write`/`unlink` bloqueiam se `os.state ∉ {draft,scheduled}` E vals tocam campos de agenda (`date/time_*/planned_hours/tecnico/os/equip/instrum`); permite marcar `done` e editar nota; (3) **equipamento sobreposto** `@api.constrains('equipment_ids','date_start','date_stop')` — bloqueia mesmo equip + janela sobreposta (hard ValidationError, ≠ conflito instrumento que é aviso). **Polish (board xml/scss):** avatar do técnico `/web/image/hr.employee/<id>/avatar_128` (24px redondo) na coluna; observações (`note`) no card (ícone fa-sticky-note-o, itálico, clamp 2 linhas) + `note` no payload `board_fetch`. **Fix diálogo:** equip/instrument grids `<tree create="false" edit="false">`+opts → SelectCreateDialog sem botão NEW. **Testes:** 10 novos no `test_visita.py` (passado/OS-travada/equip-overlap) + **converteu TODAS as datas hardcoded 2026-06-1x dos 4 arquivos de teste p/ relativas** (`context_today+30d`) — suite não envelhece com a regra de passado. **61 testes verdes em db limpa.** **E2E browser (agent-browser):** SO→configurador (Autoclave QI/QO/QS+QD 5 ciclos=10h)→Aplicar→Confirm (gerou OS+qualifs)→Sugerir visitas (2 visitas [8h,2h])→board. **Blocker corrigido:** `column sale_order_line.optional_id does not exist` no db → `-u afr_qualificacao` (campo existia no código, faltava no schema). Achado: nome OS deriva do SO (`C..`→`OS..`) idempotente; colidiu com seed `OS26-06-0001` na db poluída (não é bug). Dados E2E: partner Hospital E2E Fluxo (809) + equip Autoclave AV-E2E (788). Doc `docs/quadro_agenda_manual_check.md` (secções 1-8 PASS).
- 2026-06-06 — **afr_qualificacao_agendamento — board card polish completo** (branch `main-monorepo`, último `fec4f07`, pushed). Card do Quadro de Agenda: **lixeira** vermelha → modal ConfirmationDialog; **horário início–fim editável inline** (board_set_hours, recalcula horas); **faixa "+" full-height** à direita quando passa do dia (board_split_overflow, continuação mesmo horário início + resto; reaparece se ainda extrapola); **ícones FontAwesome** (trash-o/plus/check/wrench/tachometer-amarelo/exclamation-triangle/chevrons); **equipamento** apelido→tag→nome; **dropdown OS com filtro** por Nº QOS/cliente; planned_hours 1 casa decimal. Testes board_delete_visita/board_set_hours/split. **Form da visita:** domains (equip da OS via os_equipment_ids related; técnico is_tecnico), grids equip/instr read-only + no_create, "Dividir em 2 dias". **Doc teste manual:** `docs/quadro_agenda_manual_check.md` (NÃO commitado). **Pendente:** user testar no browser (qualif_test_f811).
- 2026-06-06 — **afr_qualificacao 16.0.5.18.0 — wizard revisão: quadro de opcionais selecionados** (submodule `d8e08fd`, monorepo `574c8da`). `optional_summary_html` no step revisão (serviços + qualif aceitos c/ subtotal + TOTAL). Também v16.0.5.16.0 (instrução marcar X destacada no PDF/portal + tabela "Subtotais de Opcionais aceitos" no form via _qualif_optionals_subtotals_html) e v16.0.5.17.0 (qualif opcional domain equip do cliente).
- 2026-06-05 — **afr_qualificacao 16.0.5.15.0 — Opcionais Fase 4 (portal interativo) — FEATURE COMPLETA** (submodule `f47f16b`, monorepo `8999352`). Cliente marca opcionais no portal: `sale.order._portal_toggle_optional` (valida draft/sent + pertença + is_proposal_optional, grava optional_accepted + sync); controller token-auth POST `/my/orders/<id>/optional/<line>/toggle` (`_document_check_access`/consteq); checkbox interativo no portal (só draft/sent) + JS publicWidget (`ajax.jsonRpc` + reload). 5 testes. Security review OK (auth/IDOR/state-gate). **Opcionais COMPLETO: F1 núcleo + F2 wizard + F3 PDF/portal + F4 interativo.** labquali precisa `-u`.
- 2026-06-05 — **afr_qualificacao 16.0.5.14.0 — Opcionais Fase 3 (PDF/portal: preço ref. + caixa ☑/☐)** (submodule `f9efdf1`, monorepo `b05c727`). Campo `optional_ref_subtotal` (price_unit × qty-se-aceito) mostra o preço da oferta mesmo não aceita. 3 templates (block_optionals PDF + portal + snapshot _html_optionals): coluna ☑/☐ (`optional_accepted`), Qtd=optional_qty, Subtotal (ref.), nota. Total geral inalterado (só aceitos somam). 4 testes. **labquali precisa `-u`.**
- 2026-06-05 — **afr_qualificacao 16.0.5.13.0 — Opcionais Fase 2 (step "Opcionais" no wizard)** (submodule `52295e4`, monorepo `8f522ee`). Fluxo escopo→opcionais→revisão. 2 transient models (`configurator.optional` serviço do catálogo + `.optional.qualif` qualificação extra) + O2M no header; `action_apply` gera linhas `is_proposal_optional`; FK `optional_id` em sale.order.line persiste catálogo (evita perda no reapply se arquivado); `_load_from_existing_lines` repopula no re-open. Testes configurator_steps atualizados p/ 3 etapas. 6 testes novos. **labquali precisa `-u`.**
- 2026-06-04 — **afr_qualificacao 16.0.5.12.0 — Opcionais Fase 1 (só conta se aceito)** (submodule `1a27083`, monorepo `0a19c5a`). `optional_accepted`+`optional_qty` em sale.order.line; qty=0 até aceito (não soma); helper `_optional_target_qty()` (não aceito→0; aceito+ciclo/malha→horas; aceito+serviço→optional_qty); confirm gera opcional aceito c/ qualification_type (qualif+OS); toggle no form. 8 testes. Bug sub-faturação apanhado em review. ⚠️ opcionais existentes deixam de somar até aceites (intencional).
- 2026-06-04 — **afr_qualificacao 16.0.5.11.0 — Rótulo de tempo por tipo de processo** (submodule `18927a1`, monorepo `c53a457`). Campo `process_type` (esterilizacao/lavagem/desinfeccao/monitoramento) em engc.equipment.category; coluna da tabela de ciclos vira "Tempo de Esterilização/Lavagem/Desinfecção/Ciclo" conforme equip. Seed: lavadora=lavagem, termo=desinfeccao, refrig=monitoramento. 5 testes. ⚠️ labquali precisa `-u` + setar categorias.
- 2026-06-04 — **afr_qualificacao_agendamento — polish board + add técnico/visita** (branch `main-monorepo`, 126deb8→ea99161, pushed). Barra rica, conflito border-left, coluna "hoje", done não-arrastável; campo `is_tecnico`; botões "＋ Técnico"/"+" por célula com dropdowns OWL (position:fixed); bugfix card stale; domain equip por cliente; hora-fim auto+warning; lista equip/instr vertical. Bugs OWL: `t-att-draggable` string; `min-height` ignorado em table-cell; `useExternalListener` de `@odoo/owl`.
- 2026-06-04 — **Cotação C26-06-0001 no labquali (draft, via MCP)**. 2 equips placeholder (cliente id 49) via wizard. Pendente: corrigir cliente/equip, temp ultrassônica 45 vs 60°C. NÃO confirmada.
- 2026-06-03 — **afr_qualificacao_agendamento Fases A-D** (commits fcc178e → 11090b1 → 8c36c5b, monorepo `main-monorepo`). Módulo satélite (`depends: afr_qualificacao`): Fase A = modelo `afr.qualificacao.os.visita` (OS×dia×técnico), rollup `date_planned_*`, gate `action_schedule`, conflitos técnico+deslocamento (não-bloqueantes), calendar nativa + aba Visitas + menu "Agenda de Visitas"; Fase B = motor assistivo `_suggest_visitas` (wizard + botão "Sugerir visitas", distribui técnico-dias F5.8.0 por bloco/parallel_group, dias corridos), depreca calendar antiga da OS; Fase C = `instrument_ids` (M2m manual) + conflito instrumento duplo + calibração vencida + botão "Puxar do plano F10"; Fase D = board OWL Gantt técnico×dia (`ir.actions.client afr_qualif_visita_board`), drag reagenda data+técnico, clique abre form, date pickers + nav. 30 testes automáticos pass. **C+D não testados manualmente** — dados de teste criados em `qualif_test_f811`.
- 2026-06-03 — **afr_qualificacao 16.0.5.10.0 — Sequência SO/OS `C[YY]-[MM]-NNNN`** (submodule `b4b56e5`, monorepo `2ebfc9c`). SO passa a usar sequência `afr.qualificacao.sale.order` com `use_date_range=True` (reset anual em janeiro), formato `C26-06-0001`. OS criada na confirmação deriva nome substituindo `C`→`OS` (`OS26-06-0001`). Collision guard: se OS desvinculada tiver mesmo nome, é re-linkada (não duplicada). SQL unique constraint `(name, company_id)` em `afr.qualificacao.os`. OS criada diretamente (sem SO) mantém sequência `QOS`. 8 novos testes em `test_sequence_naming.py`; suite 324 verde (3 fails pré-existentes). **Requer `-u afr_qualificacao`** para aplicar constraint + sequência no DB.
- 2026-06-02 — **afr_qualificacao 16.0.5.9.0 — Partes 01/02 QI/QO + declínio + escopo tabela hierárquica + texto box editável** (submodule `c69393f..b2fd9eb`, pushado `65a19eb`→`b2fd9eb`→`a8ce0b9` monorepo). Feature completa testada e pushada. QI/QO: atributo "Parte" {01,02}, variantes, preço do variante. Declínio: qty=0, fora total/qualif/horas/cronograma, selo "NÃO SOLICITADO EXECUÇÃO". Configurador: `do_qo_part01`+flags declínio+validação. **Relatório escopo = tabela hierárquica por equipamento** (numeração a./a.1/a.1.1, header equip como linha, coluna Parte c/ rowspan, box declínio agrupado por equip, sem coluna Qtd). PDF+portal paridade. Migration 5.9.0 repoint simétrico. **Texto do box "Itens Não Solicitados" agora vem de `afr.proposal.section` (`sec_declined_items_box`, `noupdate=1`)** — editável em Qualificações → Biblioteca de Seções, propaga global. `quotation_template.xml` refatorado 1227→529 linhas, blocos em `reports/templates_blocos/` (10 templates via t-call). ~20 testes novos. Suite 248 verde (3 fails pré-existentes). **Para deploy na labquali:** `-u afr_qualificacao` (migration roda) + cadastrar preço Parte 01 nos variantes QI/QO (`product_qi_service_tmpl`/`product_qo_service_tmpl`). Root cause bug render: PDF usa QWeb inline (`templates_blocos/`), não builder Python `_html_equipment_scope` (snapshot/edição); builder mantém formato bullets (caminho separado).
- 2026-06-01 — **afr_qualificacao 16.0.5.8.0 — Jornada de trabalho (h/dia) por equipamento** (submodule `d14b93e`, monorepo `826d710`). Divisor de dias úteis deixa de ser fixo `/8`: campo `work_hours_per_day` (default 8) em `config.template` + linha do configurador (puxa do template via onchange, **editável por equipamento**) + snapshot na section line do SO. Helper `sale.order._qualif_work_hours_per_day(equip)` (lê section line, fallback 8) substitui os `/8.0` em `_qualif_estimated_days`/`_qualif_schedule_rows`/`qualif_subtotals_html`(total)/`proposal_block._html_schedule`. Total de dias = **soma por equipamento**. Coluna **"h/dia"** só nas tabelas de cronograma (PDF + portal + `_html_schedule`); nota fixa "8 horas/dia" removida. Bulk wizard usa 8 fixo. 5 testes novos (`tests/test_work_hours_per_day.py`); suite 270 verde (só 3 fails pré-existentes). Executado via subagent-driven (T1-T5, spec+quality review por task). Spec/plano em `docs/superpowers/{specs,plans}/2026-06-01-jornada-trabalho*`. **Aguarda teste do user (-u no ambiente).**
- 2026-06-01 — **Bug horas da malha de calibração — INVESTIGADO, não era código.** Onchange do configurador (`_onchange_malha_type_defaults`) carrega `estimated_hours` corretamente (provado por teste `test_malha_type_default_propagates_to_subline_via_onchange`). Causa = DADO: malha com `estimated_hours=0`. Local: todas 0 (drift do seed `noupdate`). labquali: seeded (Temp/Press/Hum)=4h OK, só **"Temporização" (id 4)=0h** → user vai verificar/setar no remoto. Sem mudança de código necessária.
- 2026-06-01 — **afr_qualificacao 16.0.5.7.0** — duas entregas (commit submodule `809f22c`, monorepo bump `3cc9974`, deployadas em labquali via `-u`). (1) **Seed QI/QS** `afr.qualificacao.type.config` no `post_init_hook` (produtos serviço + config por empresa) → configurador QI/QS funciona em fresh-install sem `UserError`. (2) **Proposta Venda + Calibração** (3º arquétipo, doc 4 LabQuali): novo `block_kind` `sales_items` (tabela Descrição|Quant|Unit|Total das linhas do SO, totais self-consistent) no relatório de cotação; gate de layout estendido (`has_qualif_lines OR proposal_block_ids.filtered('included')`) p/ SO de venda pura usar layout dedicado; seed XML com 9 seções institucionais + template `TPL-VENDA-CALIB`; **bloco "Dados Cadastrais — LabQuali" padrão em TODAS as propostas** via helper idempotente `_ensure_company_data_block` (hook fresh-install + `migrations/16.0.5.7.0/post-migrate.py` em `-u`). 12 testes novos; suite 262 verde (só 3 fails pré-existentes/ambientais). Executado via subagent-driven-development (4 tasks TDD + spec/code-quality review por task). Spec/plano em `docs/superpowers/{specs,plans}/2026-05-31-proposta-venda-calibracao*`. **Verificado live labquali (MCP):** bloco nos 2 templates; SOs existentes precisam "Recarregar Blocos" p/ pegá-lo.
- 2026-05-29 — **afr_brasil_lookup 16.0.1.0.0** (novo módulo). Adiciona em `res.partner` dois lookups públicos: **CEP** via `brasilapi.com.br/api/cep/v2/{CEP}` (preenche street/street2/city/state_id/zip/country_id) e **CNPJ** via `open.cnpja.com/office/{cnpj}` (preenche name/vat/is_company/endereço completo/state_id/zip/phone formatado BR/email). Triggers: botões "Buscar CEP"/"Buscar CNPJ" no form (sempre) + onchange opt-in via ICPs `afr_brasil_lookup.auto_fill_cep`/`auto_fill_cnpj` (configuráveis em Settings > Geral > AFR Brasil Lookup). CNPJ validado pelos dígitos verificadores antes de chamar API. HTTP via stdlib `urllib.request` (timeout 5s, sem deps externas). Erros mapeados pra `UserError` BR-friendly (404, 429 rate limit, rede). **11/11 tests PASS** (mock `_http_get_json` cobre invalid length, not-found, populates address, invalid check digit, rate limit, onchange disabled/enabled by ICP). Validado live em DB `brl_smoke`: CEP 01310-100 → Avenida Paulista/Bela Vista/SP; CNPJ 33.000.167/0001-01 → Petrobras/RJ/+55 21 2166-0000/email. **Pendente:** teste UI do user → promoção a submodule público `github.com/afonsorcarvalho/afr_brasil_lookup`.
- 2026-05-29 — **Repo `odoo-qualificacao-deploy` (público)** criado para deploy single-cliente em servidor de produção. Stack Odoo 16 + Postgres 14 + Dockerfile slim (só docxtpl + deps mínimas, sem ECM/NFe/LLM bloat). Bundled: `engc_os`, `engc_equipments`, `engc_fcm`, `afr_labquali_layout`, `steril_stock`, `web_domain_field` (privados copiados do monorepo). Submodule público: `addons/afr_qualificacao`. Inclui `conf/odoo.conf` hardened (list_db=False, proxy_mode, workers=4), `nginx/odoo.conf.example` (HTTPS via certbot), `scripts/install.sh`/`backup.sh`/`restore.sh`, README com passo-a-passo. URL: `github.com/afonsorcarvalho/odoo-qualificacao-deploy`. Commits: `b39be22` (initial) + `4763369` (fix .gitignore data/ que estava ignorando addons/*/data/ ⇒ install de afr_labquali_layout e engc_os falhava por XMLs faltando). Repo tornado público depois (com aviso explícito sobre exposição dos addons AFR privados).
- 2026-05-28 — **afr_cme_rastreabilidade — piloto kiosk Expurgo (Tasks 1-11)**: redesign do totem OWL para modo kiosk dedicado no posto Expurgo (mantém back-office para outras estações). Activado por `?kiosk=1`, `context.cme_totem_kiosk:true` ou ICP `cme.totem.kiosk_default`. **Tasks entregues:** (1) 3 ICPs + `totem_get_app_config` expõe `kiosk_default/sound_enabled/idle_timeout_min` + 2 smoke tests; (2) detecção kiosk + classe root + topbar Odoo oculta + full-bleed; (3) KioskTopbar (estação + pill setor·método·dono + relógio + dot conexão) + KioskStepper (1 Setup → 2 Scan → 3 Concluir); (4) split 60/40 + ScanHero + painel "+1 nome" 1.5s + cleanup timers; (5) Web Audio beep 880Hz ok / 220Hz err + flash overlay 200ms verde/vermelho; (6) MaterialsList coluna direita com row height 56px fixo + blink amarelo 2× na linha recém-adicionada + font +20% + qty editável inline + merge backend de linhas mesmo catalog_item; (7) ActionFooter + listener atalhos F1-F5 + handlers; (8) ConfirmModal antes "Concluir" com getters `expurgoBlockReasons`/`canConcluirExpurgo` + Enter confirma / Esc fecha / modal bloqueia atalhos; (9) setup ocultar pós-sessão + drawer F3 esquerda 420px com backdrop + Help modal F1 + auto-foco scan robusto (onPatched + visibilitychange + guards anti-rouba-foco); (10) idle overlay teal gradient após `idle_timeout_min` (default 5min) + relógio 30s + ping conexão `/web/webclient/version_info` 30s + activity listeners; (11) checklist manual em `docs/cme_totem_kiosk_manual_check.md`. **Extras:** cancelar sessão F8 + RPC `totem_cancel_receipt` (só rascunhos), navegação ↓↑ Enter nos resultados de busca, scan/search inputs preservam valor + select all após RPC. Commits submodule: 90c08fe → 894b080 → 97ce308 → cf933f7 → acfb8b0 → 68d78bf + Task 11. Monorepo bumps: a2d096e → 2898492 → e1fce15 → eecc40b → 53a07e4 → 9750dd5 → f636221 → 01f78aa. QUnit tests deferidos (sem infra no projeto). [[project-afr-cme-rastreabilidade]]
- 2026-05-28 — afr_cme_rastreabilidade* promovido a submodule. Repo standalone `github.com/afonsorcarvalho/afr_cme_rastreabilidade` (private) com os 3 módulos (core + supervisorio + demo). Submodule montado em `addons/afr_cme/`. `addons_path` em `conf/odoo.conf` recebeu `/mnt/extra-addons/afr_cme`. Container web restart OK; em `odoo-steriliza` DB (módulos installed) Odoo encontra manifests no novo path (`latest_version=16.0.1.0.1` preservado). README + LICENSE LGPL-3 + .gitignore criados no repo standalone. Commit inicial `8951a54`.
- 2026-05-24 — F9.1 LabQuali brand layout (afr_labquali_layout 16.0.1.0.0). Novo addon registra tile "LabQuali" no wizard Configure Document Layout (sequence 10). Template QWeb com header (logo + divisor azul), footer (border-top azul + page number), Inter font bundled woff2, selo CONFIDENCIAL opcional via `res.company.lq_confidential_default` (related em `base.document.layout` para preview wizard). Estilo Boxed-base aplicado (tabelas com border 1px cinza, thead UPPERCASE fundo cinza claro + texto azul, total row fundo azul + texto branco). H2 + h2 span em laranja como acento. Paperformat A4 bundled noupdate=1 (margin_top=20 margin_bottom=10 margin_left=5 margin_right=8 header_spacing=20). Header/footer SEM `<table>` (float-based) para evitar bleed de borders de outros reports (invoice/inventory). Stripe lateral laranja REMOVIDO. Stale SCSS asset deletado (estava injetando border laranja no footer via web.report_assets_common). Commits no monorepo: 5a86948, 85adbf1, ccb7556, e842da1, d7b94f9, b2aed7a, 598699e, 7adc289, b83972d.

## Pendente

### afr_qualificacao
- **Testar Quadro de Agenda no browser** (db qualif_test_f811): seguir `addons/afr_qualificacao_agendamento/docs/quadro_agenda_manual_check.md` — card (lixeira/modal, horários editáveis, faixa split, ícones FA, filtro OS, equip apelido→tag→nome), form da visita (domains, grids read-only, dividir em 2 dias), conflitos.
- **Testar aceitar + assinar proposta como CLIENTE (não admin)**: portal com token/sessão não-admin — marcar opcionais (checkbox F4) + Aceitar/Assinar — confirmar sem permissões de admin (token, draft/sent, total recalcula).
- **Deploy labquali**: `-u afr_qualificacao` (F1-F4 opcionais + process_type v5.11 + setar categorias lavagem/desinfeccao/monitoramento) + `-u afr_qualificacao_agendamento`. labquali ainda em versão antiga.
- **afr_qualificacao_agendamento — polish backlog**: strings do board sem `_t` (i18n); matriz cidade→cidade deslocamento (hoje buffer manual); opção pular fim de semana no motor de sugestão.
- **F4.2 — Timesheet/project + custos extras**: dep `hr_timesheet`. `project_id`/`task_id` auto via produto `service_tracking=task_in_project`. `cost_total` compute de `account.analytic.line`. Wizard "Adicionar custo" (deslocamento/instrumento).
- **F4.4 — Multi-company hardening + testes**: record rules por `company_id`, multi-company support, suite `tests/test_billing.py` adicional.
- **F4.5 (novo)** — Auto-geração `engc.calibration` ao approval de qualif Calib + propagação state bidirecional. Hoje link é manual (Opção A do plan).
- **Proposta Venda+Calib — `_find_mail_template`** gated em `has_qualif_lines` → SO venda+calib usa template de email Odoo padrão, não o LabQuali. Estender p/ `or proposal_block_ids.filtered('included')` se quiser email LabQuali nesses SOs.
- **Proposta Venda+Calib — texto SEC-ACEITE** cita "Qualificação Térmica e Calibração", impreciso p/ venda pura (editável noupdate; ajustar redação ou criar seção aceite dedicada).
- **3º arquétipo doc 3 — Proposta de Calibração pura** (sem venda): tabela já suportada por `sales_items`; falta template seed dedicado (ver `docs/Exemplos diversos/3 - Proposta de Calibração - LabQuali.doc`).
- **Portal — proposta Venda+Calibração não renderiza** (pendente): (a) ✅ "esteril"→"esterilização" feito; (b) gate do portal só em `has_qualif_lines` → estender p/ `or proposal_block_ids.filtered('included')` (senão venda+calib cai no layout Odoo padrão); (c) falta branch `sales_items` no dispatch do portal `sale_order_portal_template.xml` (tabela em branco). Bump alvo 16.0.5.x.
- ~~**Opcionais (F1-F4)**~~ — **COMPLETO** (v16.0.5.12.0 → 15.0, ver Feito). Pendente só: teste do user no browser + `-u afr_qualificacao` no labquali.
- **Desconto nos totais por equipamento** — campo de desconto por equipamento em **percentagem** E **valor absoluto**, sincronizados (alterar um recalcula o outro). Aplicar no subtotal do equipamento na cotação/proposta.

### afr_llm_assistant
- **Cache catálogos** (`_installed_models_catalog_text`, `_reports_catalog_for_user_text`, `_whitelist_models_field_catalog`): atualmente rebuild a cada call (~2-5s overhead). Usar `@tools.ormcache` ou cache em memória com invalidação por write em `ir.model` / `ir.actions.report`.
- **Streaming SSE** no chat: substituir RPC bloqueante por stream de tokens (Ollama suporta `stream: true`). Melhora UX (sem "AGUARDE..." parado). Requer adapt no `_lmstudio_chat_*` + frontend OWL consumir EventSource.
- **WebSocket bind error 8072** (`KeyError: 'socket'` polui logs): configurar gevent worker pra `/websocket` OR desabilitar `/websocket` para o chat (usar polling do job, que já funciona).
- **Timeout HTTP configurável**: `urllib.request.urlopen(req, timeout=180)` hardcoded em `_lmstudio_post_json`. Mover pra ICP `afr_llm_assistant.http_timeout`.
- **Tool `odoo_make_link`**: estruturar geração de URLs `/web#…`, `/web/content/<id>`, `/report/pdf/<name>/<ids>` via tool call (substitui `[[ODOO_LINK]]` legacy).
- **Whitelist expansiva por grupo**: ICP atual é global. Permitir por grupo de user via `res.groups` (ex.: gerentes acessam mais modelos).
- **Embeddings + RAG (F1 do roadmap)**: pgvector já no compose; indexar histórico de OS, manuais técnicos, anexos. Usar `nomic-embed-text` ou `qwen2.5:1.5b` pra embeddings.
- **Métricas/observabilidade**: log estruturado tokens in/out, latência, modelo, cache hit. Dashboard simples (kanban ou dashboard ir.ui.view).
- **Histórico múltiplas sessões**: hoje 1 sessão por user. Permitir N com título auto-gerado pelo modelo.
- **Tool calling para Anthropic provider**: implementação atual só lmstudio. Anthropic Claude tem `tools` array nativo similar.

### afr_cme_rastreabilidade*
- **Testes do fluxo crítico** — zero cobertura. Prioridade: (a) lote IQ+IB libera só com `pass`+`negativo`; (b) retrabalho cria novo `cme.process.lot` com `parent_lot_id`; (c) `reuse_count` > `max_reuse` bloqueia; (d) `cron_check_expired_units` marca expired; (e) `_check_can_distribute` bloqueia + log; (f) modo `third_party` exige `material_owner_partner_id`.
- **Mapping RDC 15/2012** — manifest reivindica conformidade mas sem evidência no código. Documentar quais artigos cada regra atende (IQ obrigatório, validade, rastreabilidade, retrabalho).
- **Promover `db_schema_patches.py`** — SQL bruto idempotente no `post_init_hook` (coluna `preparation_id` em `cme_sterilization_batch`) indica drift histórico. Mover para migration formal `16.0.1.0.1/pre-` e remover hook.
- **README + CHANGELOG** — sem docs. Documentar arquitetura (jornada lote + estados trace_unit), fluxos totem (5 estações), bridge supervisório.
- **Seed produção** — só há demo. Criar dados mínimos para go-live (sem demo).
- **Mapping IQ no bridge supervisório** — `action_sync_from_supervisorio_ciclo` só sincroniza IB. IQ permanece manual. Avaliar adicionar campo IQ no `afr.supervisorio.ciclos` e propagar.

### Novos módulos (utilidades BR)
- **`afr_brasil_lookup` — promover a submodule público** (pós-aprovação UI do user). Repo standalone `github.com/afonsorcarvalho/afr_brasil_lookup` + add no monorepo + add em `odoo-qualificacao-deploy`.

### Outros
- afr_ecm: criar `data/dms_access_group_data.xml` com `dms.access.group` default vinculando `group_ecm_user`, `group_ecm_manager` e `group_ecm_admin` em `group_ids`. Sem isso, novos users ECM não veem diretórios DMS até ajuste manual via UI/SQL. Workaround atual: SQL direto na DB.
- F4.1.3 Electron real: terminar watch folder (já tem useWatchFolder + settingsStore + settings UI). Falta testar fim-a-fim com WSLg/GUI.
- ecm_desktop TOC PDF: aba "Sumário" abrir por default no FilePreviewModal (atualmente só auto-abre se outline existe + multi-pg; mudar pra sempre default quando há entries).
- ecm_desktop TOC PDF: clique em item do sumário deve scrollar até a linha do título dentro da página, não só pular pra página. Usar coords do dest (Y offset) via `pdf.getDestination` + `react-pdf` scroll offset (scrollIntoView do span com coords) ou destacar bbox.
- ecm_desktop Search PDF: resultado de busca scrollar pra linha exata do match (mesma técnica do TOC). Hoje pula só pra página, user tem que rolar manual.
- ecm_desktop Grid pastas: quando pasta atual tiver subpastas, mostrar ícone de pasta clicável no grid (entra na subpasta) junto com os arquivos. Hoje subpastas só aparecem no FolderTree esquerdo.

## Feito
- 2026-05-16 — afr_qualificacao v16.0.2.2.2: PDF cotação refinements — (a) agrupa Sumário Executivo + Descritivo Técnico mesma página; (b) agrupa Resumo Financeiro + Condições Comerciais + Aceite mesma página (classe CSS `qq-section-cont`); (c) esconde `.address` injetado por `external_layout_standard` na capa (eliminava duplicação partner antes do título); (d) duplicação SO preserva metadados qualif (`copy=True` em equipment_id, qualification_type, cycle_type_id, malha_type_id, is_qualificacao_managed; `afr_qualificacao_id` mantém `copy=False`). 43 tests pass.
- 2026-05-16 — afr_qualificacao v16.0.2.2.0: relatório PDF dedicado de cotação (inherit `sale.report_saleorder_document` com fallback condicional via `has_qualif_lines`): capa + sumário executivo + descritivo técnico + escopo por equipamento + normas aplicáveis + resumo financeiro + condições comerciais + aceite. Novo modelo `afr.qualificacao.standard` + M2M em cycle_type/malha_type + 8 normas seed. Section lines por equipamento no form SO + painel HTML `qualif_subtotals_html`. Fonts +30%.
- 2026-05-16 — afr_qualificacao F4.3 v16.0.2.1.0: certificado digital verificável (hash SHA-256 + token UUID4 + QR + controller `/qualificacao/verify/<token>`). Templates QI/QO/QD/QS + inherit engc.calibration + fallback Calib sem `engc.calibration`. Commit `936b444`.
- 2026-05-16 — afr_qualificacao v16.0.2.0.0: fluxo comercial quote-first (sale.order → afr.qualificacao + engc.os). Wizard configurador (matriz equipamento × tipo) + sub-records cycle/malha + integração engc.calibration via FK manual + propagação qty_delivered no approval. 21 tests pass. Commit `655eb09`.
- 2026-05-15 — afr_llm_assistant: GPU NVIDIA passthrough (nvidia-container-toolkit + compose) + modelo custom `llama3.1:8b-ctx8k` (num_ctx=8192) + system prompt refactor anti-SQL/few-shot. CPU 6.5 tok/s → GPU ~100 tok/s.
- 2026-05-15 — afr_llm_assistant: tool calling estruturado (OpenAI tools API) opt-in via ICP `use_tool_calling`. 4 tools (search_read, search_count, read_group, fields_get) + coerção tolerante args + loop multi-turn max 5 iter. Resolve hallucination multi-turn. System prompt enxuto na variante tool-calling (~60% menor).
- 2026-05-12 — Conversão de subtree para git submodules (afr_ecm + ecm_desktop). Backup em tag `pre-submodule-conversion` e branch `backup/pre-submodule-conversion`.
- 2026-05-12 — Sidebar esquerda redimensionável (splitter drag, persist localStorage).
- 2026-05-12 — Drag-drop mover arquivos e pastas no FolderTree (com bloqueio de ciclo).
- 2026-05-12 — Editor de propriedades inline no painel direito (tipo, pasta, confid, OCR, vencimento) com permission_write check.
- 2026-05-12 — `ocr_enabled` per-file no afr_ecm (opt-in override do tipo).
- 2026-05-12 — Ícones por tipo de arquivo + thumbnails para imagens (lib/file-icons + FileIcon).
- 2026-05-12 — Renomear pasta (modal + F2 + dblclick + Pencil hover).
- 2026-05-12 — Delete pasta (Trash hover + Shift+Del + confirm).
- 2026-05-12 — Fix criar pasta raiz com storage_id + group_ids.
- 2026-05-12 — Sort dropdown + Empty state + atalhos Enter/Del.
- 2026-05-12 — Nova pasta + Breadcrumb + Tags (m2m dms.tag).
- 2026-05-12 — Restrição de download por tipo de documento (afr_ecm + ecm_desktop).
